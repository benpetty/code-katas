language: python
sudo: required
dist: xenial
python:
  - "3.7"

# command to install dependencies
install:
  - pip install -r requirements.txt


# command to run tests
script:
  - make test

notifications:
  email: false

after_success:
  - coveralls
